# fetchmail setting

defaults
mda "/usr/bin/procmail -d %T"
set postmaster "forrest"

poll pop.gmail.com with proto POP3 port 995 user "NNNN@nn.com" with pass "NNN" is forrest here options keep ssl sslcertck sslcertpath /home/forrest/.ssl_certs


